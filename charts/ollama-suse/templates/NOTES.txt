1. Get the application URL by running these commands:
{{- if and .Values.ollama.service (contains "NodePort" .Values.ollama.service.type) }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "ollama-suse.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if and .Values.ollama.service (contains "LoadBalancer" .Values.ollama.service.type) }}
     NOTE: It may take a few minutes for the LoadBalancer IP to be available.
           You can watch the status of by running 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "ollama-suse.fullname" . }}'
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "ollama-suse.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo http://$SERVICE_IP:{{ .Values.ollama.ollama.port }}
{{- else }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "ollama-suse.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visit http://127.0.0.1:11434 to use your application"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 11434:$CONTAINER_PORT
{{- end }}

2. Test Ollama functionality:
   curl http://127.0.0.1:11434/api/tags

3. Enterprise model management:
   # List available enterprise models
   curl http://127.0.0.1:11434/api/tags

   # Pull a SUSE-certified model
   curl http://127.0.0.1:11434/api/pull -d '{"name":"enterprise-model:latest"}'

4. Enterprise API usage:
   curl http://127.0.0.1:11434/api/generate -d '{"model":"enterprise-model","prompt":"Enterprise AI query","stream":false}'

SUSE Application Collection Ollama has been deployed successfully!

🏢 Enterprise Features:

{{- if .Values.ollama.ollama.gpu.enabled }}
GPU Support: ✅ Enabled ({{ .Values.ollama.ollama.gpu.type }}, {{ .Values.ollama.ollama.gpu.number }} GPU(s))
{{- else }}
GPU Support: ❌ Disabled
{{- end }}

{{- if .Values.ollama.persistentVolume.enabled }}
Persistent Storage: ✅ Enabled ({{ .Values.ollama.persistentVolume.size }}, {{ .Values.ollama.persistentVolume.storageClass }})
{{- else }}
Persistent Storage: ❌ Disabled
{{- end }}

For enterprise support, contact your SUSE representative.
