apiVersion: v1
kind: Service
metadata:
  name: {{ include "comfyui-video-lab.fullname" . }}
  labels:
    {{- include "comfyui-video-lab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "comfyui-video-lab.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
